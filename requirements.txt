# LEO-MINI dependencies
# Install: pip install -r requirements.txt

# ── Core framework ──────────────────────────────────────────────────────────
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# ── HuggingFace ─────────────────────────────────────────────────────────────
# transformers>=4.43.0 required for Phi-3.5-mini-instruct (Phi3ForCausalLM
# was merged into transformers at 4.43; 4.40 will raise ImportError for Phi).
transformers>=4.43.0
# accelerate>=0.30.0 aligns with transformers>=4.43 and supports device_map="auto"
# for quantised models without the extra DeepSpeed dependency.
accelerate>=0.30.0
peft>=0.10.0
datasets>=2.18.0
tokenizers>=0.19.0

# ── Quantisation (4-bit / 8-bit Adam, for Colab and memory-constrained runs) ─
bitsandbytes>=0.43.0

# ── Distributed training ─────────────────────────────────────────────────────
deepspeed==0.14.0

# ── Vision models ───────────────────────────────────────────────────────────
timm==0.9.16
open-clip-torch>=2.24.0
einops>=0.7.0
Pillow>=10.0.0

# ── Evaluation ───────────────────────────────────────────────────────────────
lmms-eval>=0.2.0

# ── Utilities ────────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0
scipy>=1.11.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.66.0
tensorboard>=2.14.0
